export default function App() {
  return (
    <main className="placeholder">
      <h1>terraform-learning</h1>
      <p>
        Interactive Terraform fundamentals tutorial. This is the Phase 0 scaffold
        — the real UI lands in subsequent phases (content rendering, editor,
        validators, observation flow).
      </p>
      <p className="footer">
        Static markdown tutorial is still available — see <code>00-START-HERE.md</code> in the repo.
      </p>
    </main>
  );
}
